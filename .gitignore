# Salesforce CLI
.sf/
.sfdx/
# macOS
.DS_Store
# Local tooling
.atl/
# Claude Code harness — .claude/skills IS tracked; local machinery is not
.claude/worktrees/
.claude/settings.local.json
# Planning workspace (local only, never committed)
openspec/
# Plan docs (local only)
plan/
.cache/
config/user/
hardis-report/
tmp/
# Node / LWC Jest tooling — __tests__ files ARE tracked (see .forceignore for
# excluding them from org deployment); only build output is ignored here.
node_modules/
/coverage/
**/cleanDataServices/
**/siteDotComSites/*.site
**/data/**/source/**
**/data/**/target/**
force-app/main/default/appMenus/AppSwitcher.appMenu-meta.xml
# Editor tooling (auto-generated by the Salesforce VS Code LWC extension)
**/jsconfig.json
